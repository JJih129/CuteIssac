namespace CuteIssac.Room
{
    /// <summary>
    /// Minimal room encounter states for the prototype.
    /// </summary>
    public enum RoomState
    {
        Idle = 0,
        Combat = 1,
        Cleared = 2
    }
}
