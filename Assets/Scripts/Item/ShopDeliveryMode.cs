namespace CuteIssac.Item
{
    /// <summary>
    /// Controls whether a purchased reward is granted immediately or materialized as a pickup in the room.
    /// </summary>
    public enum ShopDeliveryMode
    {
        Immediate = 0,
        SpawnPickup = 1
    }
}
